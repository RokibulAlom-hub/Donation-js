/********************here we have to work with common funtionalities */

// funtion of collecting input value

function forinputvalue(id){
    const values = document.getElementById(id).value;
    return values;
}